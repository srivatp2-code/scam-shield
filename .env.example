# Scam Shield is fully client-side and uses BYOK (Bring Your Own Key).
# The user's Anthropic API key is entered in the app UI and stored in their
# browser's localStorage. No build-time or runtime environment variables are
# required.
#
# This file is kept as a placeholder to signal that env vars were considered
# and intentionally not used.
